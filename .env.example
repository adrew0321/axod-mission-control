# Anthropic API key — required for agent runtime (week 2+)
ANTHROPIC_API_KEY=

# SQLite database file path (relative to repo root or absolute)
DATABASE_PATH=./data/mission-control.db

# 32+ byte random string — used to sign session cookies (week 1 day 3+)
SESSION_SECRET=
